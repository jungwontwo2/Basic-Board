spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/Basic-Board?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8&allowPublicKetRetrieval=true
    username: root
    password: root
  http:
    encoding:
      charset: UTF-8

  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
    database: mysql
    hibernate:
      ddl-auto: create
  mustache:
    servlet:
      expose-request-attributes: true

# ?? ?? ? ?? ??? ?? ??
server:
  port: 8080
  servlet:
    session:
      tracking-modes: cookie
    encoding:
      force-response: true
  tomcat:
    uri-encoding: UTF-8
file:
  profileImagePath: C:/Temp/profileImageUpload/
  boardImagePath: C:/Temp/boardImageUpload/